
# ✅ Eleventy 项目上线 GitHub（指令版）

## 前提

* 已在本地完成内容 / 样式修改
* 当前在项目根目录（有 `package.json`、`src/`、`.git/`）

---

## ①（可选）本地预览确认

```bash
npm run serve
```

浏览器打开 `http://localhost:8080`
确认页面、样式无误后继续。

---

## ② 查看当前修改状态

```bash
git status
```

确认：

* 修改的文件在 `modified`
* 新文件在 `Untracked files`

---

## ③ 添加所有改动

```bash
git add -A
```

---

## ④ 提交一次上线记录

```bash
git commit -m "Update site content and styles"
```

（commit message 可按实际改动改）

---

## ⑤ 推送到 GitHub（触发自动部署）

```bash
git push
```

> 👉 这一行 = **正式上线按钮**

---

## ⑥ 确认是否发布成功

1. 打开 GitHub 仓库
2. 点击 **Actions**
3. 查看最新 workflow：

   * 🟢 绿色 = 上线成功
   * 🟡 In progress = 正在部署
   * 🔴 红色 = 失败（查看日志）

---

## ⑦ 查看线上效果

* 打开官网
* `Ctrl + F5` 强制刷新（清缓存）

---

# ⚠️ 重要注意事项（长期有效）

* ❌ 不要手动修改或提交 `_site/`
* ✅ 只改 `src/` 下内容
* ✅ GitHub Pages 由 Actions 自动生成
* ✅ `.md` 改内容，`.css` 改样式，`.njk` 改结构

---

## 🧠 一句话心智模型

> **改内容 → git add → git commit → git push → Actions 自动上线**

---


